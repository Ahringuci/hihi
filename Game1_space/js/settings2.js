// ----------+----------+---------- > CAMERA
const camera = {
    w: 900,
    h: 600,
    pad: 100,
};

// ----------+----------+---------- > FONT SETTINGS
const font = {
    thin: "14px Consolas",
    normal: "16px Consolas",
    bigger: "20px Consolas",
    stage: "100px Consolas",
};

// ----------+----------+---------- > ID
const counter = {
    id: 0,
};

export const settings = {
    camera,
    font,
    counter,
};
